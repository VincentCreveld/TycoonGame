using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraManager : MonoBehaviour
{
    // Check for max distance backward
	// Move smoothly and snap if LoS is broken.
	// Rotate on right mouse
	// Zoom in/out (move cam max dist closer)
}
